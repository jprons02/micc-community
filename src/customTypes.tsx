export type UserType = {
  firstName: string;
  lastName: string;
  email: string;
  tribalId: string;
  password: string;
};

export type LoginFormType = {
  email: string;
  tribalId: string;
  password: string;
};
